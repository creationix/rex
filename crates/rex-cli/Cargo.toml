[package]
name = "rex-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "rex"
path = "src/main.rs"

[dependencies]
rex-core.workspace = true
clap.workspace = true
lsp-server = "0.7"
lsp-types = "0.97"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
